//const { Dog } = require('../../db/index');

//module.exports = {
  //createBreed: async (req, res, next) => {
    //try {
      //const { breed } = req.body;
      //if (!breed.name) res.status(500).send('The name is required.');

     //const createdBreed = Dog.create({
        //name: breed.name,
        //height: breed.height,
      //});
      //res.json(createdBreed);
    //} catch (e) {
      //next(e);
    //}
//}
